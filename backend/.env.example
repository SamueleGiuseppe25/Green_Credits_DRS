# Backend environment

# Provide a Postgres URL for real DB, e.g.:
# postgresql+asyncpg://gc:gc@localhost:5432/greencredits
DATABASE_URL=

# Opt-in dev-only SQLite fallback when DATABASE_URL is empty
USE_SQLITE_DEV=false

# Dev secret key (placeholder)
SECRET_KEY=dev-secret
